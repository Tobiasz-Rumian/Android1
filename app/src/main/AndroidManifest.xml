<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.project1">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".ProductViewActivity"
            android:parentActivityName=".ProductListActivity">
            <intent-filter>
                <action android:name="com.example.project1.ProductViewActivity" />
            </intent-filter>
        </activity>
        <activity
            android:name=".OptionsActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".ProductListActivity"
            android:parentActivityName=".MainActivity">
            <intent-filter>
                <action android:name="com.example.project1.ProductListActivity" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".provider.MainContentProvider"
            android:authorities="com.example.project1.provider.MainContentProvider"
            android:exported="true" />
    </application>

</manifest>